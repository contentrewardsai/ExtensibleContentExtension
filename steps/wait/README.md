# Wait

## Testing

### Unit tests (step-tests.js)

- duration when both min and max (fixed duration)
- duration in range (random between durationMin and durationMax)
- duration fallback (uses action.duration or 1000)
