{
  "id": "getShotstackCredits",
  "label": "Get ShotStack Credits",
  "description": "Check remaining ShotStack render credits and usage. Requires backend login.",
  "category": "data",
  "defaultAction": {
    "type": "getShotstackCredits",
    "runIf": "",
    "saveAsVariable": "shotstackCredits"
  },
  "inputs": [],
  "outputs": [
    { "key": "saveAsVariable", "description": "Row variable to store { credits, usedSeconds }" }
  ],
  "formSchema": [
    { "key": "runIf", "label": "Run only if (optional)", "inputType": "text", "placeholder": "{{var}} or variable name", "hint": "Skip step when empty/falsy." },
    { "key": "saveAsVariable", "label": "Save to variable", "inputType": "text", "placeholder": "shotstackCredits" }
  ]
}
