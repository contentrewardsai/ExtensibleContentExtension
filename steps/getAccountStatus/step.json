{
  "id": "getAccountStatus",
  "label": "Get Account Status",
  "description": "Get account status including plan tier, upgrade status, connected profiles, ShotStack credits, storage quota, and login state.",
  "category": "data",
  "defaultAction": {
    "type": "getAccountStatus",
    "runIf": "",
    "saveAsVariable": "accountStatus"
  },
  "inputs": [],
  "outputs": [
    { "key": "saveAsVariable", "description": "Row variable to store account status object" }
  ],
  "formSchema": [
    { "key": "runIf", "label": "Run only if (optional)", "inputType": "text", "placeholder": "{{var}} or variable name", "hint": "Skip step when empty/falsy." },
    { "key": "saveAsVariable", "label": "Save to variable", "inputType": "text", "placeholder": "accountStatus" }
  ]
}
