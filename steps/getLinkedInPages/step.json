{
  "id": "getLinkedInPages",
  "label": "Get LinkedIn Pages",
  "category": "data",
  "description": "Fetch LinkedIn company pages (organizations) from connected Upload Post accounts. Returns organization URNs, names, vanity URLs, and logos.",
  "defaultAction": {
    "type": "getLinkedInPages",
    "runIf": "",
    "apiKeyVariableKey": "uploadPostApiKey",
    "profileVariableKey": "",
    "saveAsVariable": "linkedinPages"
  },
  "inputs": [
    { "key": "apiKeyVariableKey", "description": "Row variable for Upload Post API key" },
    { "key": "profileVariableKey", "description": "Row variable for profile username (optional)" }
  ],
  "outputs": [
    { "key": "saveAsVariable", "description": "Row variable to store the array of LinkedIn pages" }
  ],
  "formSchema": [
    { "key": "runIf", "label": "Run only if (optional)", "inputType": "text", "placeholder": "{{var}} or variable name", "hint": "Skip step when empty/falsy." },
    { "key": "apiKeyVariableKey", "label": "API key variable key", "inputType": "text", "placeholder": "uploadPostApiKey" },
    { "key": "profileVariableKey", "label": "Profile variable key (optional)", "inputType": "text", "placeholder": "", "hint": "Filter results to a specific profile." },
    { "key": "saveAsVariable", "label": "Save response to variable", "inputType": "text", "placeholder": "linkedinPages" }
  ]
}
