{
  "id": "getStorageInfo",
  "label": "Get Storage Info",
  "description": "Check backend storage usage, quota, and file count. Requires backend login.",
  "category": "data",
  "defaultAction": {
    "type": "getStorageInfo",
    "runIf": "",
    "saveAsVariable": "storageInfo"
  },
  "inputs": [],
  "outputs": [
    { "key": "saveAsVariable", "description": "Row variable to store { usedBytes, limitBytes, fileCount, percentUsed }" }
  ],
  "formSchema": [
    { "key": "runIf", "label": "Run only if (optional)", "inputType": "text", "placeholder": "{{var}} or variable name", "hint": "Skip step when empty/falsy." },
    { "key": "saveAsVariable", "label": "Save to variable", "inputType": "text", "placeholder": "storageInfo" }
  ]
}
